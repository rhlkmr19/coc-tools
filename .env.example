# ============================================
# Clash Intelligence Pro – Environment Variables
# ============================================
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.
# ============================================

# OpenRouter AI API Key (OPTIONAL – needed for AI strategy features)
# Get yours at: https://openrouter.ai/keys
VITE_OPENROUTER_API_KEY=your_openrouter_api_key_here

# Clash of Clans API Token (OPTIONAL – NOT needed when using CORS proxy)
# The default CORS proxy (cocproxy.royaleapi.dev) handles auth internally.
# Only set this if you're calling api.clashofclans.com directly.
# VITE_COC_API_TOKEN=your_coc_api_token_here

# CORS Proxy URL for CoC API (default: cocproxy.royaleapi.dev)
# The official CoC API does not support browser CORS.
# Community proxies handle both CORS and authentication.
VITE_COC_API_BASE=https://cocproxy.royaleapi.dev/v1
